package com.prog.hud.Repo;

import com.prog.hud.Types.group;
import org.springframework.data.mongodb.repository.MongoRepository;

public interface group_repo extends MongoRepository<group, String> {
}
